import { style } from '@vanilla-extract/css';

export const AdjustableNumber = style({
  textDecorationStyle: 'dotted',
  textDecorationLine: 'underline',
  userSelect: 'none'
});

export const AdjustableContainer = style({
  display: 'inline-block'
})
